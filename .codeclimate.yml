---
engines:
  shellcheck:
    enabled: true
    # turn off client side expansion warning, as we explicitly want it
    checks:
      SC2029:
        enabled: false
      SC2016:
        enabled: false
      SC2086:
        enabled: false
ratings:
  paths:
  - "utils/aws-*"
exclude_paths:
  - "install.sh"
  - "utils/aws-quick-cli-init.sh"
